/*
  Declares global definitions for simple functions to be used in lexer implementation.
*/


#ifndef MACROS_H
#define MACROS_H


#define MAX(a, b)\
  a > b ? a : b

  #define MIN(a, b)\
    a < b ? a : b

#endif
